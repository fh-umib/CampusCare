import{Router}from'express';import{authenticate}from'../../middleware/authenticate.js';import{asyncHandler}from'../../utils/asyncHandler.js';import{supportController}from'./support.controller.js';
export const supportRoutes=Router();supportRoutes.use(authenticate);supportRoutes.get('/conversations',asyncHandler(supportController.list));supportRoutes.get('/conversations/:id',asyncHandler(supportController.get));supportRoutes.post('/conversations/:id/join',asyncHandler(supportController.join));supportRoutes.get('/conversations/:id/messages',asyncHandler(supportController.get));supportRoutes.post('/conversations/:id/messages',asyncHandler(supportController.message));supportRoutes.patch('/conversations/:id/read',asyncHandler(supportController.read));supportRoutes.patch('/conversations/:id/close',asyncHandler(supportController.close));
